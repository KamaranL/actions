name: 'release'
description: ''
runs:
  using: composite
  steps:
    - run: |
        ls -lA
      shell: bash

    # - if: ${{ github. }}
      # uses: actions/checkout@v4

    - uses: actions/checkout@v4

    - run: |
        ls -lA
      shell: bash
