mode: ContinuousDeployment
assembly-informational-format: '{MajorMinorPatch}-{env:CI_PRERELEASE_LABEL}+Branch.{env:CI_TARGET_BRANCH}.Sha.{env:CI_SOURCE_SHA}'
branches:
  pull-request:
    tag: '{env:CI_PRERELEASE_LABEL}'
    tag-number-pattern: ''
ignore:
  sha: []
merge-message-formats: {}
