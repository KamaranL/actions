name: github-release
description: ''
inputs:
  assets:
    description: ''

runs:
  using: composite
  steps:
    - run: $GITHUB_ACTION_PATH/pre.sh
      shell: bash

    - run: $GITHUB_ACTION_PATH/main.sh
      shell: bash
      env:
        INPUTS_ASSETS: ${{ inputs.assets }}
